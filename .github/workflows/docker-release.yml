name: Docker Release

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-release-${{ github.event.release.tag_name || github.ref_name || github.run_id }}
  cancel-in-progress: false

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/gslide
  MOVING_TAG: dev

jobs:
  build-and-push:
    name: Build and push ${{ matrix.component }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: web
            dockerfile: Dockerfile.web
            image_suffix: -web
          - component: api
            dockerfile: Dockerfile.api
            image_suffix: -api

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare Docker tags
        id: docker-tags
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "release" ]]; then
            IMAGE_TAG="${{ github.event.release.tag_name }}"
          else
            IMAGE_TAG="${MOVING_TAG}"
          fi
          IMAGE="${IMAGE_NAME}${{ matrix.image_suffix }}"

          {
            echo "tags<<EOF"
            echo "${IMAGE}:${IMAGE_TAG}"
            echo "EOF"
            echo "verify_image=${IMAGE}"
            echo "verify_tag=${IMAGE_TAG}"
          } >> "${GITHUB_OUTPUT}"

      - name: Build and push multi-architecture image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.docker-tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image
        env:
          VERIFY_IMAGE: ${{ steps.docker-tags.outputs.verify_image }}
          VERIFY_TAG: ${{ steps.docker-tags.outputs.verify_tag }}
        run: |
          docker buildx imagetools inspect "${VERIFY_IMAGE}:${VERIFY_TAG}" | tee image-inspect.txt
          grep -q "linux/amd64" image-inspect.txt
          grep -q "linux/arm64" image-inspect.txt
